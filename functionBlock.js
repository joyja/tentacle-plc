const { path } = require('path')
const fs = require('fs')
const Mqtt = require('./mqtt')

class FunctionBlock {
  constructor() {
    this.constructor.definition
  }
}

module.exports = {
  FunctionBlock,
}
